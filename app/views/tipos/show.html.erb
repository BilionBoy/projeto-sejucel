<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar Tipo</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @tipo %>">
          <p>
            <strong>Descricao:</strong>
            <%= @tipo.descricao %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_tipo_path(@tipo), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", tipos_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @tipo, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
