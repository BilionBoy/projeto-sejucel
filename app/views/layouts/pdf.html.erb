<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "Relatório" %></title>

    <style>
      body {
        font-family: "Helvetica", Arial, sans-serif;
        font-size: 12px;
        color: #333;
        margin: 20px;
      }

      .page-break {
        page-break-after: always;
      }

      .crachas-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
      }

      .cracha {
        border: 1px solid #ccc;
        border-radius: 10px;
        padding: 15px;
        width: 250px;
        text-align: center;
      }

      .cracha h2 {
        margin: 0 0 5px;
        font-size: 16px;
      }

      .cracha p {
        margin: 3px 0;
        font-size: 12px;
      }

      .qrcode {
        margin-top: 10px;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
