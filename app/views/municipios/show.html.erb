<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar Municipio</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @municipio %>">
          <p>
            <strong>Descricao:</strong>
            <%= @municipio.descricao %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_municipio_path(@municipio), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", municipios_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @municipio, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
